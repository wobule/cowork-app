# Turso Database (required for cloud deployment, optional for local dev)
# Get your free database at https://turso.tech
TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
TURSO_AUTH_TOKEN=your-auth-token

# If not set, falls back to local SQLite file at data/mission-control.db
